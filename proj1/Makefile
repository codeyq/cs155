submission:
	tar cvzf submission.tar.gz \
	  --exclude-backups --exclude='sploit[1-6]' --exclude=run-shellcode \
	  --exclude='*.o' --exclude='*.bin' --ignore-failed-read \
	  sploits/ ID.csv fuzz/README fuzz/results/fuzzer_stats fuzz/results/crashes/
